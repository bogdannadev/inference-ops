apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Service name on the shared compose network.
    url: http://prometheus:9090
    isDefault: true
    editable: true