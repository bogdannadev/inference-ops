# =============================================================================
# QWEN3.6-27B BF16 — SGLANG PATTERN A
# 2× TP=1 replicas + cache-aware router + EAGLE (MTP) speculative decoding
# + HiCache + Caddy edge gateway + segmented networks
#
# Host:
#   2× A100 80GB PCIe (P2P: NS — hence two TP=1 replicas, no TP=2)
#   AMD EPYC 7663, NPS2, SMT off: cores 0-27 NUMA0, 28-55 NUMA1
#   1 TB RAM (HiCache host tier)
#
# Design:
#   - Two independent TP=1 replicas, one per GPU, no inter-GPU comms
#   - Router: cache-aware routing (prefix locality per worker)
#   - Networks: `edge` (external, shared) carries Caddy <-> router;
#     `qwen36-27b-backend` (project-owned) carries router <-> workers.
#     Caddy cannot reach worker ports / ZMQ sockets (CVE-2026-3059/3060
#     lateral-movement hardening).
#   - Only Caddy publishes host ports (80/443)
#
# HiCache:
#   ENABLED. The old hybrid Mamba/Attention crash
#   ("Destination indices must be a CUDA tensor") was retired by the
#   UnifiedRadixTree refactor (v0.5.13+, PR #27759). Flag set below is the
#   compatibility-first pairing (direct / page_first_direct) validated on
#   this node. --hicache-size is PER RANK, in GB of host RAM.
#
# Speculative decoding:
#   EAGLE against the model's bundled MTP head (mtp.safetensors), per the
#   official Qwen3.6 cookbook. BOOT CHECK on every start (issue #29857):
#   read the `Memory pool end` line — if the profiled max_total_num_tokens
#   has collapsed (~50 GB VRAM stranded), drop the four --speculative-*
#   flags and reboot clean before investigating.
#
# Metrics:
#   Observability tier lives in compose.metrics.yml — SAME project.
#   After first combined `up`, ALWAYS pass both files to compose commands:
#     docker compose -f docker-compose.yml -f compose.metrics.yml <cmd>
#
# Gateway target (for any future external consumer on `edge`):
#   http://qwen36-27b-router:8000
# =============================================================================

# PIN BY DIGEST before production (guide §2):
#   docker inspect --format='{{index .RepoDigests 0}}' lmsysorg/sglang:dev-cu13
x-sglang-image: &sglang-image lmsysorg/sglang:dev-cu13   # -> dev-cu13@sha256:...

x-replica-common: &replica-common
  image: *sglang-image

  ipc: host
  shm_size: "32g"

  # SYS_NICE: lets the scheduler set thread affinity/priority sanely on the
  # dual-NUMA topology (both GPUs report NUMA affinity to nodes 0-1).
  cap_add:
    - SYS_NICE

  ulimits:
    memlock: -1
    stack: 67108864

  # Default SIGKILL grace is 10s. Workers need time to drain in-flight
  # generations and finish HiCache write-through before termination.
  stop_grace_period: 120s

  environment:
    - PYTHONUNBUFFERED=1
    - PYTHONWARNINGS=ignore::SyntaxWarning
    - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

    # Hugging Face auth
    - HF_TOKEN=${HF_TOKEN:?set HF_TOKEN in .env}
    - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:?set HF_TOKEN in .env}

    # SGLang OpenAI-compatible API auth (internal key; Caddy swaps the
    # edge key for this one — clients never see it)
    - SGLANG_API_KEY=${SGLANG_API_KEY:?set SGLANG_API_KEY in .env}

  entrypoint: sglang serve

  restart: unless-stopped

  logging:
    driver: "json-file"
    options:
      max-size: "50m"
      max-file: "5"

  networks:
    - qwen36-27b-backend

# NOTE on flag duplication: the two replica command blocks below are
# intentionally identical except for --port. YAML cannot splice an anchor
# into the middle of a command string, so keep them in sync BY HAND — any
# flag change must be applied to both blocks.
#
# VERIFY AGAINST THE LIVE IMAGE (guide §2) — three unsettled names:
#   1. --api-key vs --api-keys        (must be identical on both replicas)
#   2. --mamba-radix-cache-strategy vs --mamba-scheduler-strategy
#      (cookbook documents the latter; keep whichever `--help` accepts)
#   3. worker-side /health bypass flag, if the build has one

services:
  # ---------------------------------------------------------------------------
  # Replica 0 — physical GPU 0, NUMA node 0 cores
  # Internal endpoint: http://qwen36-27b-r0:8001
  #
  # Flag-group rationale (applies to both replicas) — VERIFIED 2026-07-28
  # against the live process (/get_server_info) and the boot log, not assumed.
  #
  #   context/tokens : --context-length 184320 is a guard against the
  #                    ~244K-token requests seen in the outage.
  #                    CAVEAT: --max-total-tokens 190000 is NOT what the
  #                    engine uses. Memory profiling wins, and the real pool
  #                    is max_total_num_tokens=117760 — 66,560 SHORT of the
  #                    declared context window. A request between 117,760 and
  #                    184,320 tokens passes the length check and then cannot
  #                    be allocated. Closing that gap needs a higher
  #                    --mem-fraction-static, not a bigger --max-total-tokens
  #                    (which the docs call a debugging knob / ceiling).
  #
  #   prefill/decode : chunked-prefill and max-prefill aligned at 16384
  #                    (previous 32768/16384 split meant the effective
  #                    chunk was 16384 anyway — one number, stated once).
  #                    --max-running-requests 2 (raised from 1 on 2026-07-28):
  #                    at ~65 KB/token, two concurrent 36K-token requests use
  #                    72K of the 117,760-token pool and leave ~46K for the
  #                    prefix cache. THREE would leave only ~10K and thrash
  #                    the cache — do not raise past 2 without more KV.
  #
  #   cuda graph     : --cuda-graph-max-bs-decode is CLAMPED to
  #                    max-running-requests. At max-running-requests 1 the boot
  #                    log captured bs=[1] for target-verify, draft-decode and
  #                    draft-extend despite this flag saying 2 — so there was
  #                    never any "pre-paid headroom". It becomes real only now
  #                    that max-running-requests is 2.
  #
  #   queueing       : --max-queued-requests 32 is the ONLY admission control
  #                    in the stack (the router's token bucket was removed —
  #                    see the router block). Without it the worker queue is
  #                    unbounded and overload turns into a silent wait until
  #                    --request-timeout-secs 900 fires. Bounded queue => fast
  #                    503 instead of a 15-minute stall.
  #
  #   mixed-chunk    : REMOVED 2026-07-28. It never took effect — boot log:
  #                    "Mixed chunked prefill is disabled because of using
  #                    eagle speculative decoding." Do not re-add while EAGLE
  #                    is on; it is silently ignored.
  # ---------------------------------------------------------------------------
  qwen36-27b-r0:
    <<: *replica-common
    container_name: qwen36-27b-r0

    # NUMA discipline: r0 owns node-0 cores; 48-55 stay reserved for
    # TEI + observability.
    cpuset: "0-27"

    expose:
      - "8001"

    volumes:
      - ${HF_HOME:-~/.cache/huggingface}:/root/.cache/huggingface
      - ./logs/r0:/app/logs
      - /etc/localtime:/etc/localtime:ro

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0"]
              capabilities: [gpu]

    # NOTE: `command: >` is a FOLDED SCALAR — a `#` line inside it is literal
    # argv text, not a comment, and compose rejects the file. Keep annotations
    # out here, above the key.
    #
    # REJECTED 2026-07-28: --enable-gdn-replayssm-spec (RFC #28511 Part B).
    # Preconditions looked right (GDN model, --speculative-eagle-topk 1), but
    # the build refuses it outright:
    #   ValueError: --enable-gdn-replayssm-spec is not yet compatible with
    #   mamba extra_buffer (radix prefix caching); use --disable-radix-cache
    #   or --mamba-radix-cache-strategy no_buffer.
    # Buying it means surrendering radix prefix caching, which is measured at
    # 36x on this workload (16K-token prompt: 12.278s cold TTFT -> 0.338s
    # warm). Coding-agent traffic shares a system prompt, so the cache is the
    # single largest lever we have. Do not re-enable until upstream makes the
    # kernel compatible with extra_buffer.
    command: >
      --model-path Qwen/Qwen3.6-27B
      --served-model-name qwen36-27b
      --host 0.0.0.0
      --port 8001
      --trust-remote-code
      --dtype bfloat16
      --tp 1
      --context-length 184320
      --max-total-tokens 190000
      --mem-fraction-static 0.85
      --tokenizer-worker-num 1
      --attention-backend flashinfer
      --mamba-radix-cache-strategy extra_buffer
      --mamba-ssm-dtype bfloat16
      --mamba-track-interval 64
      --page-size 64
      --chunked-prefill-size 16384
      --max-prefill-tokens 16384
      --max-running-requests 2
      --max-queued-requests 32
      --cuda-graph-max-bs-decode 2
      --schedule-policy lpm
      --reasoning-parser qwen3
      --tool-call-parser qwen3_coder
      --grammar-backend xgrammar
      --enable-metrics
      --enable-mfu-metrics
      --enable-cache-report
      --speculative-algorithm EAGLE
      --speculative-num-draft-tokens 4
      --speculative-num-steps 3
      --speculative-eagle-topk 1
      --enable-hierarchical-cache
      --hicache-size 128
      --hicache-io-backend direct
      --hicache-mem-layout page_first_direct
      --hicache-write-policy write_through
      --watchdog-timeout 1200
      --api-key ${SGLANG_API_KEY}

    healthcheck:
      # /health is a GENERATION probe, not a liveness check — parameters
      # are deliberately loose so a worker mid-prefill does not flap.
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 60s
      timeout: 15s
      retries: 10
      start_period: 2400s

  # ---------------------------------------------------------------------------
  # Replica 1 — physical GPU 1, NUMA node 1 cores
  # Internal endpoint: http://qwen36-27b-r1:8002
  # Starts after r0 so one container populates the shared HF cache.
  # ---------------------------------------------------------------------------
  qwen36-27b-r1:
    <<: *replica-common
    container_name: qwen36-27b-r1

    cpuset: "28-47"

    depends_on:
      qwen36-27b-r0:
        condition: service_healthy

    expose:
      - "8002"

    volumes:
      - ${HF_HOME:-~/.cache/huggingface}:/root/.cache/huggingface
      - ./logs/r1:/app/logs
      - /etc/localtime:/etc/localtime:ro

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["1"]
              capabilities: [gpu]

    command: >
      --model-path Qwen/Qwen3.6-27B
      --served-model-name qwen36-27b
      --host 0.0.0.0
      --port 8002
      --trust-remote-code
      --dtype bfloat16
      --tp 1
      --context-length 184320
      --max-total-tokens 190000
      --mem-fraction-static 0.85
      --tokenizer-worker-num 1
      --attention-backend flashinfer
      --mamba-radix-cache-strategy extra_buffer
      --mamba-ssm-dtype bfloat16
      --mamba-track-interval 64
      --page-size 64
      --chunked-prefill-size 16384
      --max-prefill-tokens 16384
      --max-running-requests 2
      --max-queued-requests 32
      --cuda-graph-max-bs-decode 2
      --schedule-policy lpm
      --reasoning-parser qwen3
      --tool-call-parser qwen3_coder
      --grammar-backend xgrammar
      --enable-metrics
      --enable-mfu-metrics
      --enable-cache-report
      --speculative-algorithm EAGLE
      --speculative-num-draft-tokens 4
      --speculative-num-steps 3
      --speculative-eagle-topk 1
      --enable-hierarchical-cache
      --hicache-size 128
      --hicache-io-backend direct
      --hicache-mem-layout page_first_direct
      --hicache-write-policy write_through
      --watchdog-timeout 1200
      --api-key ${SGLANG_API_KEY}

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 60s
      timeout: 15s
      retries: 10
      start_period: 2400s

  # ---------------------------------------------------------------------------
  # Router — sole bridge between edge and backend
  # Internal URL: http://qwen36-27b-router:8000
  # Prometheus:   http://qwen36-27b-router:29000 (scraped by overlay)
  #
  # Policy rationale (2026-07-28, measured — see benchmarks/results/):
  #   `cache_aware` was starving r0 completely: 8/8 sequential requests with
  #   fully DISJOINT prefixes all routed to r1, r0_delta=0 every time, while
  #   both workers reported load=0 and is_healthy=true. GPU0 sat at 0% util
  #   during load. Two compounding causes:
  #     1. balance_abs_threshold defaults to 64, but with
  #        --max-running-requests 1 the load delta between workers can never
  #        exceed 1 — the rebalance escape hatch is mathematically unreachable.
  #     2. With one shared system prompt (coding-agent traffic), affinity
  #        routing WANTS to pile the whole team onto whichever replica warmed
  #        first. Cache-affinity is built for many prefixes across many
  #        workers; here it is one prefix across two.
  #   power_of_two on 2 workers == pick-the-less-loaded, so long requests do
  #   not get a second one stacked on them. The shared system prompt caches on
  #   BOTH replicas after first use, so prefix-cache benefit is retained.
  #
  # Rate-limit rationale: --max-concurrent-requests is NOT a concurrency cap.
  #   Per `launch_router --help` it is a TOKEN BUCKET whose refill rate
  #   defaults to the same number, i.e. `2` meant "2 requests per SECOND,
  #   service-wide". Bursts died with 408 + router log
  #   "Queue: request timed out waiting for token". Default is -1 (disabled);
  #   omitted here so backpressure comes from worker queues, not an
  #   arbitrary bucket. Revisit if unbounded queueing shows up under real load.
  # ---------------------------------------------------------------------------
  qwen36-27b-router:
    image: *sglang-image
    container_name: qwen36-27b-router

    expose:
      - "8000"
      - "29000"

    stop_grace_period: 30s

    environment:
      - PYTHONUNBUFFERED=1

    entrypoint: python3 -m sglang_router.launch_router

    command: >
      --host 0.0.0.0
      --port 8000
      --policy round_robin
      --worker-urls http://qwen36-27b-r0:8001 http://qwen36-27b-r1:8002
      --request-timeout-secs 900
      --prometheus-host 0.0.0.0
      --prometheus-port 29000

    depends_on:
      qwen36-27b-r0:
        condition: service_healthy
      qwen36-27b-r1:
        condition: service_healthy

    # Required for Caddy's service_healthy condition below. Short
    # start_period: the router only starts once both workers are healthy.
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

    restart: unless-stopped

    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

    networks:
      - qwen36-27b-backend   # reach workers
      - edge                 # be reachable from Caddy

  # ---------------------------------------------------------------------------
  # Caddy — TLS termination, edge auth, request-body cap
  # The ONLY service publishing host ports. Edge network only: it can reach
  # the router (and qwen3-emb from the embedding project), never a worker.
  # ---------------------------------------------------------------------------
  caddy:
    image: caddy:2-alpine

    cpuset: "48-55"

    ports:
      - "443:443"
      - "80:80"

    stop_grace_period: 30s

    environment:
      - EDGE_API_KEY=${EDGE_API_KEY:?set EDGE_API_KEY in .env}
      - SGLANG_API_KEY=${SGLANG_API_KEY:?set SGLANG_API_KEY in .env}

    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

    # service_healthy (not bare depends_on): Caddy must not serve 502s
    # during the ~40-minute cold warm-up window.
    depends_on:
      qwen36-27b-router:
        condition: service_healthy

    restart: unless-stopped

    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

    networks:
      - edge

networks:
  # Shared edge network — created ONCE outside any compose project:
  #   docker network create edge
  # Members: this Caddy, this router, and the TEI container from the
  # embedding project (so `qwen3-emb` resolves for Caddy).
  edge:
    name: edge
    external: true

  # Project-owned backend — workers + router (+ metrics overlay) only.
  qwen36-27b-backend:
    name: qwen36-27b-backend
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
